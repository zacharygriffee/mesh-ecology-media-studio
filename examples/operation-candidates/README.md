# Operation Candidate Fixture Catalog

This directory contains deterministic local examples for the existing media
operation resolver.

The catalog is used by tests to confirm that common operation candidates still
resolve to the expected local rule-resolution posture. It is not mesh truth,
operator approval, execution, provider truth, byte availability proof, or
publication authorization.
