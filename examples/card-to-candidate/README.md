# Card To Candidate Example

Run:

```bash
npm run wedge:example
```

This example is intentionally local-only. The candidate file is a text
placeholder so the wedge can run without provider credentials or binary media
assets.

Outputs are written to `out/` and labeled as local records, not mesh truth.
